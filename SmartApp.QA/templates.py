"""
Funzioni per l'analisi di frasi standard
Each sentence will be lowercased
ASt the init a dictionary is built:
key: input_sentence
value: (query to perform to kb,possible template answer to be filled)
template answer will have 1 or more placeholder eg "Lecture of Attardi is in <aula>"
"""

def __main__():
    input = "Dove si trova la Lezione del Professor Gervasi?"
    template_list = [(1, "Dove si trova la lezione del professor <C>")]
    input = input.lower()
    temp_s = template_list[]
    in_tokens 
    
